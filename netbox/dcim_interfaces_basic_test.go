// Copyright 2017 The go-netbox Authors.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// Code generated by generate_basic_tests.go. DO NOT EDIT.

package netbox

import (
	"encoding/json"
	"errors"
	"fmt"
	"net/http"
	"reflect"
	"testing"
)

// Using this to override MarshalJSON
// In all cases when posting data to netbox-API, the Interface.MarshalJSON is what you want,
// but not here as a return in testHandler
type serverDataInterface Interface

func convertToServerDataInterface(data []*Interface) []*serverDataInterface {
	dataWant := make([]*serverDataInterface, len(data))
	for i := range data {
		tmp := serverDataInterface(*data[i])
		dataWant[i] = &tmp
	}
	return dataWant
}

func TestBasicInterfaceGet(t *testing.T) {
	var tests = []struct {
		desc string
		want *Interface
	}{
		{
			desc: "Simple Interface",
			want: testInterface(1),
		},
	}

	for i, tt := range tests {
		t.Run(fmt.Sprintf("[%d] %s", i, tt.desc), func(t *testing.T) {
			serverData := serverDataInterface(*tt.want)

			c, done := testClient(t, testHandler(t, http.MethodGet, "/api/dcim/interfaces/1/", &serverData))
			defer done()

			res, err := c.DCIM.Interfaces.Get(1)
			if err != nil {
				t.Fatalf("unexpected error from Client.DCIM.Interfaces.Get: %v", err)
			}

			if want, got := tt.want, res; !reflect.DeepEqual(want, got) {
				t.Fatalf("unexpected Interface:\n- want: %v\n-  got: %v", want, got)
			}
		})
	}
}

func TestBasicInterfaceGet404(t *testing.T) {
	c, done := testClient(t, testStatusHandler(t, http.MethodGet, "/api/dcim/interfaces/1/", &struct {
		Detail string `json:"detail"`
	}{
		Detail: "Not found.",
	},
		http.StatusNotFound))
	defer done()

	res, err := c.DCIM.Interfaces.Get(1)
	errstr := "404 - Not found."
	if want, got := errors.New(errstr), err; !reflect.DeepEqual(want, got) {
		t.Fatalf("unexpected error from Client.DCIM.Interfaces.Get:\n- want: %v\n-  got: %v", want, got)
	}

	if res != nil {
		t.Fatalf("unexpected result:\n- want: %v\n-  got: %v", nil, res)
	}
}

func TestBasicListExtractInterface(t *testing.T) {
	want := []*Interface{
		testInterface(1),
		testInterface(2),
	}
	serverWant := convertToServerDataInterface(want)
	serverData, _ := json.Marshal(serverWant)
	c, done := testClient(t, testHandler(t, http.MethodGet, "/api/dcim/interfaces/", &pageData{
		Count:       2,
		NextURL:     "",
		PreviousURL: "",
		Results:     serverData,
	}))
	defer done()

	page := c.DCIM.Interfaces.List(nil)

	if page == nil {
		t.Fatalf("unexpexted result from c.DCIM.Interfaces.List.")
	}

	got := []*Interface{}
	counter := 0
	for page.Next() {
		var err error
		got, err = c.DCIM.Interfaces.Extract(page)
		if err != nil {
			t.Fatalf("unexpected error from c.DCIM.Interfaces.Extract: %v", err)
		}
		counter = counter + 1
		if counter > 2 { // Safe guard
			break
		}
	}
	if counter != 1 {
		t.Fatalf("unexpected page count:\n- want: 1\n-  got: %d", counter)
	}

	if !reflect.DeepEqual(want, got) {
		t.Fatalf("unexpected result:\n- want: %v\n-  got: %v", want, got)
	}

	if page.Err() != nil {
		t.Fatalf("unexpected error from page:\n- want: %v\n-  got: %v", want, got)
	}
}

func TestBasicCreateInterface(t *testing.T) {
	var tests = []struct {
		desc       string
		data       *Interface
		want       int
		serverData interface{}
		status     int
		errstr     string
	}{
		{
			desc:       "Create with ID 0",
			data:       testInterfaceCreate(1),
			want:       1,
			status:     0,
			errstr:     "",
			serverData: testInterface(1),
		},
		{
			desc:   "Create duplicate",
			data:   testInterfaceCreate(1),
			want:   0,
			status: http.StatusBadRequest,
			errstr: "400 - {\"name\":[\"InterfacesService with this name already exists.\"]}\n",
			serverData: &struct {
				Name []string `json:"name"`
			}{
				Name: []string{"InterfacesService with this name already exists."},
			},
		},
	}

	for i, tt := range tests {
		t.Run(fmt.Sprintf("[%d] %s", i, tt.desc), func(t *testing.T) {
			c, done := testClient(t, testStatusHandler(t, http.MethodPost, "/api/dcim/interfaces/", tt.serverData, tt.status))
			defer done()

			var terr error
			if tt.errstr != "" {
				terr = errors.New(tt.errstr) // Using errstr and initialize real err here, to satisfy golint
			}

			res, err := c.DCIM.Interfaces.Create(tt.data)
			if want, got := terr, err; !reflect.DeepEqual(want, got) {
				t.Fatalf("unexpected error:\n- want: %v\n-  got: %v", want, got)
			}
			if want, got := tt.want, res; !reflect.DeepEqual(want, got) {
				t.Fatalf("unexpected Interface:\n- want: %v\n-  got: %v", want, got)
			}
		})
	}
}

func TestBasicUpdateInterface(t *testing.T) {
	var tests = []struct {
		desc       string
		data       *Interface
		want       int
		serverData interface{}
		status     int
		errstr     string
	}{
		{
			desc:       "Update with ID 1",
			data:       testInterface(1),
			want:       1,
			serverData: testInterface(1),
			status:     0,
			errstr:     "",
		},
		{
			desc: "Update not found",
			data: testInterface(1),
			want: 0,
			serverData: &struct {
				Detail string
			}{
				Detail: "Not found.",
			},
			status: http.StatusNotFound,
			errstr: "404 - Not found.",
		},
		{
			desc: "Update to duplicate",
			data: testInterface(1),
			want: 0,
			serverData: &struct {
				Name []string `json:"name"`
			}{
				Name: []string{"InterfacesService with this name already exists."},
			},
			status: http.StatusBadRequest,
			errstr: "400 - {\"name\":[\"InterfacesService with this name already exists.\"]}\n",
		},
	}

	for i, tt := range tests {
		t.Run(fmt.Sprintf("[%d] %s", i, tt.desc), func(t *testing.T) {
			c, done := testClient(t, testStatusHandler(t, http.MethodPatch, "/api/dcim/interfaces/1/", tt.serverData, tt.status))
			defer done()

			var terr error
			if tt.errstr != "" {
				terr = errors.New(tt.errstr) // Using errstr and initialize real err here, to satisfy golint
			}

			res, err := c.DCIM.Interfaces.Update(tt.data)
			if want, got := terr, err; !reflect.DeepEqual(want, got) {
				t.Fatalf("unexpected error:\n- want: %v\n-  got: %v", want, got)
			}
			if want, got := tt.want, res; !reflect.DeepEqual(want, got) {
				t.Fatalf("unexpected Interface:\n- want: %v\n-  got: %v", want, got)
			}
		})
	}
}

func TestBasicDeleteInterface(t *testing.T) {
	var tests = []struct {
		desc       string
		data       *Interface
		serverData interface{}
		status     int
		errstr     string
	}{
		{
			desc:       "Delete ID 1",
			data:       testInterface(1),
			serverData: testInterface(1),
			status:     0,
			errstr:     "",
		},
		{
			desc: "Delete not Found",
			data: testInterface(1),
			serverData: &struct {
				Detail string `json:"detail"`
			}{
				Detail: "Not found.",
			},
			status: http.StatusNotFound,
			errstr: "404 - Not found.",
		},
	}

	for i, tt := range tests {
		t.Run(fmt.Sprintf("[%d] %s", i, tt.desc), func(t *testing.T) {
			c, done := testClient(t, testStatusHandler(t, http.MethodDelete, "/api/dcim/interfaces/1/", tt.serverData, tt.status))
			defer done()

			var terr error
			if tt.errstr != "" {
				terr = errors.New(tt.errstr) // Using errstr and initialize real err here, to satisfy golint
			}

			err := c.DCIM.Interfaces.Delete(tt.data)
			if want, got := terr, err; !reflect.DeepEqual(want, got) {
				t.Fatalf("unexpected error:\n- want: %v\n-  got: %v", want, got)
			}
		})
	}
}
